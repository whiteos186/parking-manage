<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.parking.mapper.ParkingUserConfigMapper">

    <resultMap type="com.ruoyi.parking.domain.ParkingUserConfig" id="ParkingUserConfigResult">
        <id     property="id"          column="id"/>
        <result property="userId"      column="user_id"/>
        <result property="configKey"   column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="updateTime"  column="update_time"/>
    </resultMap>

    <select id="selectByUserIdAndKey" resultMap="ParkingUserConfigResult">
        select id, user_id, config_key, config_value, update_time
        from parking_user_config
        where user_id = #{userId}
          and config_key = #{configKey}
        limit 1
    </select>

    <insert id="upsert">
        insert into parking_user_config (user_id, config_key, config_value, update_time)
        values (#{userId}, #{configKey}, #{configValue}, now())
        on duplicate key update
            config_value = values(config_value),
            update_time  = values(update_time)
    </insert>

</mapper>
