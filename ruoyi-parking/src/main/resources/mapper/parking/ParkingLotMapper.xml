<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.parking.mapper.ParkingLotMapper">

    <resultMap type="ParkingLot" id="ParkingLotResult">
        <id property="lotId" column="lot_id"/>
        <result property="lotName" column="lot_name"/>
        <result property="lotAddress" column="lot_address"/>
        <result property="totalSpaceCount" column="total_space_count"/>
        <result property="availableSpaceCount" column="available_space_count"/>
        <result property="monthlyPrice" column="monthly_price"/>
        <result property="tempHourPrice" column="temp_hour_price"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectParkingLotColumns">
        select
            lot_id,
            lot_name,
            lot_address,
            total_space_count,
            available_space_count,
            monthly_price,
            temp_hour_price,
            status,
            del_flag,
            create_by,
            create_time,
            update_by,
            update_time,
            remark
        from parking_lot
    </sql>

    <select id="selectParkingLotById" parameterType="Long" resultMap="ParkingLotResult">
        <include refid="selectParkingLotColumns"/>
        where lot_id = #{lotId}
          and del_flag = '0'
    </select>

    <select id="selectParkingLotList" parameterType="ParkingLot" resultMap="ParkingLotResult">
        <include refid="selectParkingLotColumns"/>
        where del_flag = '0'
        <if test="lotName != null and lotName != ''">
            and lot_name like concat('%', #{lotName}, '%')
        </if>
        <if test="status != null and status != ''">
            and status = #{status}
        </if>
        order by lot_id desc
    </select>

    <select id="selectParkingLotOptions" resultMap="ParkingLotResult">
        select lot_id, lot_name
        from parking_lot
        where del_flag = '0'
        order by lot_name asc, lot_id asc
    </select>

    <insert id="insertParkingLot" parameterType="ParkingLot" useGeneratedKeys="true" keyProperty="lotId">
        insert into parking_lot (
            lot_name,
            lot_address,
            total_space_count,
            available_space_count,
            monthly_price,
            temp_hour_price,
            status,
            del_flag,
            create_by,
            create_time,
            remark
        ) values (
            #{lotName},
            #{lotAddress},
            #{totalSpaceCount},
            #{availableSpaceCount},
            #{monthlyPrice},
            #{tempHourPrice},
            #{status},
            '0',
            #{createBy},
            sysdate(),
            #{remark}
        )
    </insert>

    <update id="updateParkingLot" parameterType="ParkingLot">
        update parking_lot
        <set>
            <if test="lotName != null">lot_name = #{lotName},</if>
            <if test="lotAddress != null">lot_address = #{lotAddress},</if>
            <if test="totalSpaceCount != null">total_space_count = #{totalSpaceCount},</if>
            <if test="availableSpaceCount != null">available_space_count = #{availableSpaceCount},</if>
            <if test="monthlyPrice != null">monthly_price = #{monthlyPrice},</if>
            <if test="tempHourPrice != null">temp_hour_price = #{tempHourPrice},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where lot_id = #{lotId}
          and del_flag = '0'
    </update>

    <update id="deleteParkingLotByIds">
        update parking_lot
        set del_flag = '2',
            update_time = sysdate()
        where del_flag = '0'
          and lot_id in
        <foreach collection="lotIds" item="lotId" open="(" separator="," close=")">
            #{lotId}
        </foreach>
    </update>

    <update id="updateParkingLotSpaceStats">
        update parking_lot
        set total_space_count = #{totalSpaceCount},
            available_space_count = #{availableSpaceCount},
            update_time = sysdate()
            <if test="updateBy != null and updateBy != ''">
                , update_by = #{updateBy}
            </if>
        where lot_id = #{lotId}
          and del_flag = '0'
    </update>

</mapper>
